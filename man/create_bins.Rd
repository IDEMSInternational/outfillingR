% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_bins.R
\name{create_bins}
\alias{create_bins}
\title{Create Bins for Rainfall Columns}
\usage{
create_bins(data, date, rainfall = NULL, by_month = TRUE, n_bins = 5)
}
\arguments{
\item{data}{A data frame containing at least a date column and a rainfall column.}

\item{date}{A vector of dates (same length as \code{rainfall} or a column in \code{data}).}

\item{rainfall}{A numeric vector of rainfall values (same length as \code{date}) or a column in \code{data}.}

\item{by_month}{Logical. If \code{TRUE}, compute bins separately for each calendar month.}

\item{n_bins}{Integer. The number of bins to create using quantile-based binning.}
}
\value{
If \code{by_month = TRUE}, returns a data frame with one row per month and a column \code{quantiles}
containing the bin edges as a list-column. If \code{by_month = FALSE}, returns a numeric vector
of quantile-based bin edges.
}
\description{
Computes bin edges for rainfall using quantiles, either across all data or
grouped by month.
}
